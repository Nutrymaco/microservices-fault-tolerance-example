2 2/4 status(Status status) : ResponseBuilder
 Create a new ResponseBuilder with the supplied status. 
 * Parameters:
   - status the response status.
 * Returns:
   - a new response builder.
 * Throws:
   - IllegalArgumentException if status is null.
